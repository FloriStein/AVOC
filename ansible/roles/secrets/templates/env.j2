# AVOC Lokale VM — Test-Dummy-Secrets (generiert von ansible/roles/secrets, LOCALVM-05)
# ACHTUNG: Diese Werte sind FEST HINTERLEGTE TEST-DUMMY-WERTE — nur für die lokale VM (siehe
# tasks/backlog.md EPIC "Lokale Ansible-VM als Hetzner-Nachbildung"). Kein echtes
# internet-exponiertes System, keine echten Nutzerdaten. chmod 600 — diese Datei existiert nur
# auf der VM, wird nicht ins Repo committet.
# Erstellt: {{ ansible_date_time.iso8601 | default('unbekannt') }} von ansible/roles/secrets

REGISTRY={{ avoc_secrets_registry }}
VERSION={{ avoc_secrets_version }}

JWT_SECRET={{ avoc_secrets_jwt_secret }}
DB_PASSWORD={{ avoc_secrets_db_password }}
ADMIN_PASSWORD={{ avoc_secrets_admin_password }}
WHIP_STREAM_KEY={{ avoc_secrets_whip_stream_key }}

TURN_EXTERNAL_IP={{ avoc_turn_external_ip }}
TURN_REALM={{ avoc_secrets_turn_realm }}
TURN_USER={{ avoc_secrets_turn_user }}
TURN_PASSWORD={{ avoc_secrets_turn_password }}

GRAFANA_ADMIN_USER={{ avoc_secrets_grafana_admin_user }}
GRAFANA_ADMIN_PASSWORD={{ avoc_secrets_grafana_admin_password }}

DOCKER_USERNAME={{ avoc_secrets_docker_username }}
DOCKER_PASSWORD={{ avoc_secrets_docker_password }}

MQTT_USERNAME={{ avoc_secrets_mqtt_username }}
MQTT_PASSWORD={{ avoc_secrets_mqtt_password }}
