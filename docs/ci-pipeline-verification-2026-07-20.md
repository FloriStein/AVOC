# CI-Pipeline-Verifikationslauf (2026-07-20)

Zweck dieser Datei: minimaler, harmloser Diff, um einen echten `pull_request`-Event auszulösen und
damit die 5 in Sprint 41 ("CI-Gates einführen", `tasks/backlog.md`) gebauten GitHub-Actions-
Workflows erstmals gegen echte GitHub-gehostete Runner laufen zu lassen (CIGATE-07 war bisher nur
lokal verifiziert, nie gegen einen echten Actions-Lauf, siehe `DECISIONS.MD`).

Diese Datei (und dieser Branch) sind reiner Testzweck — kann nach erfolgreicher Verifikation ohne
Merge wieder gelöscht/geschlossen werden.
